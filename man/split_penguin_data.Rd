% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_penguin_data.R
\name{split_penguin_data}
\alias{split_penguin_data}
\title{Split Penguin Data into Training and Testing Sets}
\usage{
split_penguin_data(data, prop = 0.75, seed = 123)
}
\arguments{
\item{data}{A data frame containing the cleaned and prepared penguin data.}

\item{prop}{Proportion of the data to include in the training set. Defaults to 0.75.}

\item{seed}{An optional random seed for reproducibility. Defaults to 123.}
}
\value{
A list with two elements: \code{train} (training data) and \code{test} (testing data).
}
\description{
This function splits the penguin data into training and testing sets,
stratified by species to maintain class balance.
}
\examples{
modeling_data <- prepare_penguin_data(load_penguin_data())
splits <- split_penguin_data(modeling_data)
train_data <- splits$train
test_data <- splits$test
}
